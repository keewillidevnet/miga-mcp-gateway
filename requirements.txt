# MIGA — Core Dependencies
# pip install -r requirements.txt

# MCP Framework
fastmcp>=2.0.0

# HTTP Client
httpx>=0.27.0

# Data Validation
pydantic>=2.5.0

# Redis (inter-service messaging)
redis[hiredis]>=5.0.0

# Authentication
PyJWT>=2.8.0
cryptography>=41.0.0

# WebEx Bot
aiohttp>=3.9.0
# MCP client used by the WebEx bot to call the gateway (do not rely on the
# gateway's install; fastmcp bundles mcp but the bot declares it explicitly).
mcp>=1.0.0

# CLI
click>=8.1.0

# AGNTCY Directory Python SDK (agntcy.dir_sdk) — native gRPC client to dir-apiserver.
# NOT on PyPI. Install from the buf.build generated-code index:
#   uv add agntcy-dir --index https://buf.build/gen/python
#   (pip: pip install agntcy-dir --extra-index-url https://buf.build/gen/python)
# Version 1.3.0 confirmed via live introspection on a Mac (dir(Client) +
# inspect.signature). dirctl is NOT required at runtime (SDK needs it only for
# signing, which MIGA omits). See VERIFY_DIRECTORY.md.
agntcy-dir==1.3.0

# INFER Intelligence Engine (optional, for infer_mcp)
# pandas>=2.1.0
# scipy>=1.11.0
# scikit-learn>=1.3.0

# Development
ruff>=0.5.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
