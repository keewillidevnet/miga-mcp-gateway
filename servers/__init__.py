"""MIGA bundled MCP servers (currently INFER).

Explicit package marker so ``servers`` is importable as a regular package in container
images that copy only part of the tree, not only as a namespace package.
"""
